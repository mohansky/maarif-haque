---
interface Props {
  columns?: 2 | 3 | 4;
}

const { columns = 2 } = Astro.props;

const gridClasses = {
  2: 'md:grid-cols-2',
  3: 'md:grid-cols-3',
  4: 'md:grid-cols-4',
};
---

<div class={`grid grid-cols-1 ${gridClasses[columns]} gap-6`}>
  <slot />
</div>
